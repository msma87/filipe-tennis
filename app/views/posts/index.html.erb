<h2 class="text-center">Últimas Novidades</h2>
<div class="row">
  <% @posts.each do |post| %>
    <div class="col-md-6">
      <div class="card mb-3">
        <div class="card-body">
          <h5 class="card-title"><%= link_to post.title, post_path(post) %></h5>
          <p class="card-text"><%= truncate(post.content, length: 150) %></p>
          <a href="<%= post_path(post) %>" class="btn btn-primary">Ler mais</a>
        </div>
      </div>
    </div>
  <% end %>
</div>
